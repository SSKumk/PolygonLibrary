# .ai

Эта папка хранит только временные рабочие материалы.

Правила:

- постоянная документация хранится в `Documentation/`;
- временные документы должны быть явно помечены;
- если временный документ перестал быть нужен, его удаляют или переносят в постоянную документацию в переработанном виде.
