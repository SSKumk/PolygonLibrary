Генерировать терминальное множество в n+1 мерном пространстве как функцию расстояния до ???
  а) до шара < -- отмасштабировали шар и обкатали в R^{n+1}
  б) до произвольного выпуклого многогранника R. Множество уровня такой функции, соответствующее константе C -- R (+) Ball(0,C)
  
  Берём сетку Сk. Считаем Conv{R (+) Ball(0,Ck), Ck}. C0 = 0;
  
  Взяли Сmax и взяли Conv{(R,0), (R (+) Ball(0, Cmax),Cmax)}, где (Polytop, Const) -- подняли многогранник на одну размерность выше с заданным значением Const последней координаты.
  
  Разумно иметь набор Ball_1 (conv{e_i,-e_i}), Ball_2 (шар обычный, параметры аппроксимации), Ball_oo (куб с гранями параллельными координатным осям [-1,+1]^n). 
  
Готово
===================================

  
А как это тестировать? --> используем 2Д наработки

) Писать доп инфу в файл многогранника! (Момент сечения)

) Тестировать производительность!




Превращаем в трехмерную:
\dot x_3 = 0
M' - кусочек надграфика функции платы.
M' = Надграфик(DistToOrigin)




ТОДО
1) Расширить конфигурационный файл
++-> Задана сама игра 
++-> Задана игра с надграфиком функции платы. (Генерировать \tilde A, B, C, M)
  
2) Расширить задание цели игры:
  - Для случая игры с терминальным множеством: 
++++> одно терминальное множество (M) (уже есть)
    > функцию платы (сетка по С) (M (+) Ball(0, C_i))
      Тут считам много раз! От разных терминальных множеств.
  - Для случая игры с надграфиком функции платы
    > функция платы

Для функций платы:
++++ > Плата = расстояние до нуля = \phi, CMax - руками.
    > Плата = расстояние до множества: Множество и CMax.
???? - М конкретного вида  (указать, потом сгенерируем в программе)
++++ - М произвольного вида (явно передать набор вершин в расширенном пространстве)

??? > Плата = произвольная выпуклая функция: ???
    - Уметь вычислять множество уровня функции платы
    - Иметь сетку по С.
    vvvv
    Вычисляем d-мерные множества (в d+1), располагаемые на каких-то уровнях, затем овыпукляем
    
3) Посчитать задачи:
+++Задачка 1: Материальная точка
\dot x_1 = x_2 + v
\dot x_2 = u
|u| <= 1, |v| <= 1   (случай 1)
              <= 0.9 (случай 2)
M = {(x_1,x_2) : sqrt(x_1 ^ 2 + x_2 ^ 2) <= Const}


+++Задачка 2: Осциллятор
\dot x_1 = x_2 + v
\dot x_2 = -x_1 + u
|u| <= 1, |v| <= 1   (случай 1)
              <= 0.9 (случай 2)
M = {(x_1,x_2) : sqrt(x_1 ^ 2 + x_2 ^ 2) <= Const}



**) Сохранять многогранники в файл?
  Посмотреть: XML-сериализация <-- сам сохранит класс.
  
==========================================================================

*** ?? Сумма в H ?? Разность в FL ?? ***

+++0) Читать (2D / 3D)-многогранник из файла

+++1) Взять dt = 0.1; Посчитать задачу 'materialDot1-0.9-supG' от 7 до 5 (хотя бы). 

+++2) Аналогично посчитать её же в двумерном случае (LDG_Convex_2D)

3) Произвести сравнение глазами и, возможно программно. Для этого рисовать в GNU plot.



Для того чтобы рисовать надо:
  На основе данных из data/ ... нарисовать фигуры в pngs/ ...
---------
    Хочу иметь "нарезку" (по С) моста для каждого момента t. Но пока так делать не буду.
    
==================================
+1) Разработать формат записи моста
  - разработать формат записи d-многогранника (используя формат ParamReader-а)
===================================
) В ParamWriter добавить запись однострочных комментариев.
) В ParamWriter добавить запись многострочных комментариев.

1) Сравнить то, что считает алгоритм мой и В.С.
   Отдельный проект:
  - берём 3Д-ConvexPolytop, рассекаем плоскостью, и печатаем в файл (x_i y_i)
2) Конвертор 2D/3D многогранника в Meshlab
  - отдельный проект
    - прочитали размерность, если не 2 и не 3, то не будем конвертировать
      если всё хорошо, то прочитали многогранник (конструктор чтения из формата), отсортировали вершины и записали в .obj или .plt формат. 
3) Симплекс-метод без целевой функции ...
    Какие грани можно вводить в базис, если мы выкинули такую-то грань.
    Можем ли мы менять на ходу целевую функцию?
=====================================================
) Погулять по графу от Фукуды и от статей HtoV. От индусов в поисках суммы.




===================
) 1 этап симплекс-метода для H2VGeometric
) Унификация индусов
) Отдельно сложить сечение моста 5.00). Отлаживаться.

=====================

Научиться нормально сравнивать решётки!