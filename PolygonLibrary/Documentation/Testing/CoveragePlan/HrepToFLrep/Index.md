# HrepToFLrep

## Scope

Класс [`HrepToFLrep.cs`](../../../../CGLibrary/Polyhedra/ConvexPolyhedra/HrepToFLrep.cs) содержит попытку геометрической конверсии из `Hrep` в `FaceLattice`.

На текущем этапе сам код прямо помечен как `!Не работает!`, а дальнейшая работа над ним перенесена в исследовательский backlog.

## Current Coverage Summary

| Topic | Status | Comment |
| --- | --- | --- |
| Archived Current Contract | `-` | Класс выведен из активного тестового слоя и оставлен как исследовательская задача в `todo.md`. |

## Archived Observations

- До вывода из активного слоя были зафиксированы два наблюдения:
  - для ограниченного 2D-политопа конвертация обрывалась `NotImplementedException`;
  - если стартовая вершина не находилась, метод возвращал `null`.
- Отдельный файл с этим архивным контрактом убран, чтобы не держать отдельную сущность без активных тестов.

## Notes

- Здесь сознательно не документируется "как должно быть", пока не будет принято решение, нужен ли этот путь отдельно от `GiftWrapping`.
