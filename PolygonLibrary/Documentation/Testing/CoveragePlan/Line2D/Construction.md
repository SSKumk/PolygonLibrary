# Line2D Construction

## Scope

Этот файл покрывает сценарии построения `Line2D` и связанные с этим инварианты:

- коэффициенты `A`, `B`, `C`;
- направляющий вектор `Direct`;
- нормаль `Normal`;
- ориентация положительной полуплоскости.

## Scenarios

| ID | Status | Scenario | Tests |
| --- | --- | --- | --- |
| `L2D-CTOR-001` | ` ` | Конструктор по умолчанию создаёт ось `Ox`. | |
| `L2D-CTOR-002` | ` ` | Конструктор по двум точкам создаёт прямую, проходящую через обе точки. | |
| `L2D-CTOR-003` | ` ` | Конструктор по двум точкам выставляет положительную полуплоскость слева от направления `p1 -> p2`. | |
| `L2D-CTOR-004` | ` ` | Конструктор по двум точкам работает для горизонтальной прямой. | |
| `L2D-CTOR-005` | ` ` | Конструктор по двум точкам работает для вертикальной прямой. | |
| `L2D-CTOR-006` | ` ` | Конструктор по двум точкам работает для наклонной прямой. | |
| `L2D-CTOR-007` | ` ` | Конструктор по двум точкам корректен при перестановке точек, если учитывать смену ориентации. | |
| `L2D-CTOR-008` | ` ` | Конструктор по двум точкам и внешней точке выбирает ориентацию так, чтобы внешняя точка лежала в положительной полуплоскости. | |
| `L2D-CTOR-009` | ` ` | Конструктор по двум точкам и внешней точке выбрасывает ошибку, если внешняя точка лежит на прямой. | |
| `L2D-CTOR-010` | ` ` | Фабрика `Line2D_PointAndDirect` создаёт прямую по точке и направлению. | |
| `L2D-CTOR-011` | ` ` | Фабрика `Line2D_PointAndDirect` с внешней точкой корректно выбирает ориентацию. | |
| `L2D-CTOR-012` | ` ` | Фабрика `Line2D_PointAndNormal` создаёт прямую по точке и нормали. | |
| `L2D-CTOR-013` | ` ` | Копирующий конструктор создаёт эквивалентную прямую. | |
| `L2D-CTOR-014` | ` ` | После построения `Normal` и `Direct` ортогональны. | |
| `L2D-CTOR-015` | ` ` | После построения `Normal` и `Direct` нормированы, если это часть фактического контракта реализации. | |
| `L2D-CTOR-016` | ` ` | Коэффициенты `A`, `B`, `C` согласованы с `Normal`. | |

## Gaps

- Нет проверок инвариантов после построения.
- Нет тестов на выбор ориентации полуплоскости.
- Нет тестов на фабричные методы.

## Notes

- При написании тестов важно проверять не только прохождение прямой через опорные точки, но и знак значения индексатора на контрольных точках по разные стороны прямой.
- В колонке `Tests` предполагаются ссылки на конкретные тестовые файлы и строки после появления явного покрытия.
