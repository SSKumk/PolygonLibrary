# Segment Intersection

## Scope

Этот файл покрывает `Segment.Intersect`, а также контракт результирующей структуры `CrossInfo`.

## Scenarios

| ID | Status | Scenario | Tests |
| --- | --- | --- | --- |
| `SEG-XP-001` | ` ` | Пересечение двух непараллельных отрезков в одной внутренней точке возвращает `CrossType.SinglePoint` и корректную точку `fp`. | |
| `SEG-XP-002` | ` ` | Пересечение в конце одного или обоих отрезков корректно размечает `IntersectPointPos`. | |
| `SEG-XP-003` | ` ` | Непересекающиеся непараллельные отрезки возвращают `CrossType.NoCross`. | |
| `SEG-XP-004` | ` ` | Коллинеарные перекрывающиеся отрезки возвращают `CrossType.Overlap` и корректные `fp`/`sp`. | |
| `SEG-XP-005` | ` ` | Коллинеарные отрезки, касающиеся в одной точке, возвращают `CrossType.SinglePoint`. | |
| `SEG-XP-006` | ` ` | Коллинеарные, но непересекающиеся отрезки возвращают `CrossType.NoCross`. | |
| `SEG-XP-007` | ` ` | Результат `Intersect(s1, s2)` согласован с `Intersect(s2, s1)` по типу пересечения и геометрии результата. | |
| `SEG-XP-008` | ` ` | Разворот порядка концов одного из отрезков не меняет геометрию пересечения. | |
| `SEG-XP-009` | ` ` | `CrossInfo` корректно заполняет ссылки `s1` и `s2` исходными объектами. | |

## Gaps

- Активного набора прямых тестов на `Intersect` сейчас нет.
- В `SegmentCrossTests.cs` есть большой закомментированный черновик, но он не участвует в текущем покрытии.

## Notes

- Для `Intersect` нужен компактный, но системный набор эталонов: крест, Т-стык, касание концами, частичное перекрытие, полное вложение, параллель без пересечения.
