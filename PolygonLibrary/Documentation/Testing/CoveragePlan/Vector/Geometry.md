# Vector Geometry

## Scope

Этот файл покрывает геометрический контракт `Vector`:

- длину и квадрат длины;
- нормировку;
- углы, косинусы и взаимное расположение;
- подпространственные операции вроде `SubVector`, `OuterProduct`, `ProjectTo2DAffineSpace`, `LiftUp`.

## Scenarios

| ID | Status | Scenario | Tests |
| --- | --- | --- | --- |
| `VEC-GEO-001` | ` ` | `Length2` равен сумме квадратов координат. | |
| `VEC-GEO-002` | ` ` | `Length` равен квадратному корню из `Length2`. | |
| `VEC-GEO-003` | ` ` | `Length` и `Length2` корректны для нулевого вектора. | |
| `VEC-GEO-004` | ` ` | `Normalize()` возвращает вектор единичной длины, сонаправленный исходному ненулевому вектору. | |
| `VEC-GEO-005` | ` ` | `NormalizeZero()` возвращает нулевой вектор для нулевого аргумента. | |
| `VEC-GEO-006` | ` ` | `NormalizeZero()` совпадает с `Normalize()` для ненулевого вектора. | |
| `VEC-GEO-007` | ` ` | `CosAngle(v1, v2)` возвращает `1` при участии нулевого вектора согласно текущему контракту реализации. | |
| `VEC-GEO-008` | ` ` | `CosAngle(v1, v2)` равен `1` для сонаправленных векторов. | |
| `VEC-GEO-009` | ` ` | `CosAngle(v1, v2)` равен `-1` для противоположно направленных векторов. | |
| `VEC-GEO-010` | ` ` | `CosAngle(v1, v2)` равен `0` для ортогональных векторов. | |
| `VEC-GEO-011` | ` ` | `Angle(v1, v2)` возвращает `0` для сонаправленных векторов. | |
| `VEC-GEO-012` | ` ` | `Angle(v1, v2)` возвращает `pi` для противоположно направленных векторов. | |
| `VEC-GEO-013` | ` ` | `Angle(v1, v2)` возвращает `pi / 2` для ортогональных векторов. | |
| `VEC-GEO-014` | ` ` | `AreParallel` возвращает `true` для сонаправленных и противонаправленных векторов. | |
| `VEC-GEO-015` | ` ` | `AreCodirected` возвращает `true` только для сонаправленных векторов. | |
| `VEC-GEO-016` | ` ` | `AreCounterdirected` возвращает `true` только для противонаправленных векторов. | |
| `VEC-GEO-017` | ` ` | `AreOrthogonal` возвращает `true` для ортогональных векторов. | |
| `VEC-GEO-018` | ` ` | `AreOrthogonal` считает нулевой вектор ортогональным любому вектору согласно текущему контракту. | |
| `VEC-GEO-019` | ` ` | `OuterProduct(v)` создаёт матрицу размера `SpaceDim x v.SpaceDim` с элементами `this[i] * v[j]`. | |
| `VEC-GEO-020` | ` ` | `SubVector(start, end)` возвращает непрерывный фрагмент координат в правильном порядке. | |
| `VEC-GEO-021` | ` ` | `LiftUp(d, val)` сохраняет исходные координаты и дополняет хвост значением `val`. | |
| `VEC-GEO-022` | ` ` | `ProjectTo2DAffineSpace(o, u1, u2)` корректно проектирует точку на заданную аффинную плоскость в простом ортонормированном случае. | |

## Gaps

- Нет явной фиксации пограничных значений для `Angle` и `CosAngle`.
- Нет отдельного покрытия для `ProjectTo2DAffineSpace`.
- Нет отдельного покрытия для `LiftUp` и `SubVector`.

## Notes

- Для углов и отношений удобнее использовать небольшие ортонормированные примеры в 2D и 3D.
- Для `ProjectTo2DAffineSpace` сначала стоит зафиксировать простой базис, где ожидаемый результат можно посчитать вручную.
