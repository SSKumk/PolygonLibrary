# Vector Construction And Identity

## Scope

Этот файл покрывает создание `Vector`, размерность, индексный доступ и базовые инварианты хранения координат.

## Scenarios

| ID | Status | Scenario | Tests |
| --- | --- | --- | --- |
| `VEC-CTOR-001` | ` ` | Конструктор `Vector(int n)` создаёт нулевой вектор размерности `n`. | |
| `VEC-CTOR-002` | ` ` | Конструктор `Vector(IEnumerable<int>)` корректно переводит целые координаты в `double`. | |
| `VEC-CTOR-003` | ` ` | Конструктор `Vector(TNum[] nv, true)` копирует входной массив. | |
| `VEC-CTOR-004` | ` ` | Конструктор `Vector(TNum[] nv, false)` использует исходный массив без копирования. | |
| `VEC-CTOR-005` | ` ` | Копирующий конструктор создаёт независимый эквивалентный объект. | |
| `VEC-CTOR-006` | ` ` | Конструктор из `Vector2D` создаёт двумерный вектор с теми же координатами. | |
| `VEC-CTOR-007` | ` ` | `SpaceDim` равен числу координат вектора. | |
| `VEC-CTOR-008` | ` ` | Индексатор возвращает правильные координаты по всем допустимым индексам. | |
| `VEC-CTOR-009` | ` ` | Явное приведение к `TNum[]` возвращает копию координат, а не внутреннее хранилище. | |
| `VEC-CTOR-010` | ` ` | `GetCopyAsArray()` возвращает массив с теми же координатами и не делит состояние с объектом. | |
| `VEC-CTOR-011` | ` ` | `IsZero` возвращает `true` для нулевого вектора. | |
| `VEC-CTOR-012` | ` ` | `IsZero` возвращает `false` для вектора с координатой вне нулевой окрестности по `Eps`. | |

## Gaps

- Пока не зафиксировано поведение copy/no-copy вариантов конструктора.
- Нет явной фиксации инварианта независимости копий.

## Notes

- Для сценариев с `needCopy` удобно использовать изменяемый массив-источник и проверять, отражаются ли его изменения в объекте.
- Для `double` полезно отдельно иметь пример с координатами около `Eps`.
