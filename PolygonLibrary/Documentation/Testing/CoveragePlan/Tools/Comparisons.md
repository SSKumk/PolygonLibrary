# Tools Comparisons

## Scope

Этот файл покрывает контракт сравнения чисел с учётом `Eps`:

- обновление `Eps` и `EpsG`;
- `CMP`, `EQ`, `NE`, `GT`, `GE`, `LT`, `LE`;
- `TNumComparer`.

## Scenarios

| ID | Status | Scenario | Tests |
| --- | --- | --- | --- |
| `TLS-CMP-001` | ` ` | Установка положительного `Eps` обновляет `EpsG` как `100 * Eps`. | |
| `TLS-CMP-002` | ` ` | `EQ(a)` считает число нулём, если `|a| < Eps`. | |
| `TLS-CMP-003` | ` ` | `EQ(a)` не считает число нулём, если `|a| >= Eps`. | |
| `TLS-CMP-004` | ` ` | `EQ(a, b)` эквивалентен проверке `EQ(a - b)`. | |
| `TLS-CMP-005` | ` ` | `NE(a, b)` является логическим отрицанием `EQ(a, b)`. | |
| `TLS-CMP-006` | ` ` | `GT(a)` возвращает `true` только для значений строго больше `Eps`. | |
| `TLS-CMP-007` | ` ` | `LT(a)` возвращает `true` только для значений строго меньше `-Eps`. | |
| `TLS-CMP-008` | ` ` | `GE(a)` считает граничное значение `-Eps` принадлежащим допустимой области. | |
| `TLS-CMP-009` | ` ` | `LE(a)` считает граничное значение `Eps` принадлежащим допустимой области. | |
| `TLS-CMP-010` | ` ` | `CMP(a)` возвращает `0` для значений в окрестности нуля по `Eps`. | |
| `TLS-CMP-011` | ` ` | `CMP(a)` возвращает `+1` для чисел строго больше `Eps`. | |
| `TLS-CMP-012` | ` ` | `CMP(a)` возвращает `-1` для чисел строго меньше `-Eps`. | |
| `TLS-CMP-013` | ` ` | `CMP(a, b)` согласован с `EQ`, `GT` и `LT` для пары чисел. | |
| `TLS-CMP-014` | ` ` | `TNumComparer.Compare(a, b)` делегирует в `CMP(a, b)` и использует текущий `Eps`. | |

## Gaps

- Нет отдельной явной фиксации поведения на границах `Eps` и `-Eps`.
- Нет изолированных сценариев на обновление `EpsG`.
- Нет отдельного прямого покрытия для `TNumComparer`.

## Notes

- Для `double` нужно обязательно иметь тесты с числами чуть меньше, ровно на границе и чуть больше границы точности.
- При написании тестов на `Eps` полезно сохранять и восстанавливать исходное значение, чтобы не создавать скрытой межтестовой зависимости.
