# Legacy Tests Data

Исторические тестовые данные и старые наборы, которые не входят в активный тестовый слой `active/CGLibrary/Tests`.

## Состав

- `Double-Tests/`
- `DoubleDouble-Tests/`

`TXTtoOBJ-convertor` сохранён внутри соответствующих наборов в `Minkowski-Tests/3D-pictures/`.
